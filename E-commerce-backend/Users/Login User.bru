meta {
  name: Login User
  type: http
  seq: 2
}

get {
  url: {{baseurl}}/users/login?email=1975ramanuj@gmail.com&password={{password}}
  body: none
  auth: none
}

params:query {
  email: 1975ramanuj@gmail.com
  password: {{password}}
}

script:post-response {
  // Save the JWT token returned by the API into the environment as 'bearer'
  // The controller returns: { token: "...", tokenType: "Bearer" }
  bru.setEnvVar("bearer", res.body.token)
}

settings {
  encodeUrl: true
}
